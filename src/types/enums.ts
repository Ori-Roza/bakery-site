/**
 * Enums for application
 */

/**
 * Sort direction
 */
export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc'
}

/**
 * Deletion confirmation types
 */
export enum DeletionType {
  PRODUCT = 'product',
  CATEGORY = 'category'
}

/**
 * User roles
 */
export enum UserRole {
  ADMIN = 'admin',
  USER = 'user'
}
